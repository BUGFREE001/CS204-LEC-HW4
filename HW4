#include <stdio.h>

int main(void)
{
    int n[3];  
    printf ("Please enter the receipts for three employees: ");
    for (int i=0; i<3; i++){
        scanf("%d", &n[i]);
    }
    
 char type_1[n[0]],type_2[n[1]],type_3[n[2]];
 float amt_1[n[0]],amt_2[n[1]], amt_3[n[2]];
 float totalA=0, totalB=0, totalC=0, sum1=0, sum2=0,sum3=0;


 printf("Please enter the %d receipt type and amounts for the #1 employee: \n", n[0]);
 for (int i=0; i<n[0]; i++){
   scanf("%s",&type_1[i]);      
   scanf("%f", &amt_1[i]);
 }

 for (int i=0; i<n[0]; i++){
   sum1 += amt_1[i];
   if (type_1[i]=='a' || type_1[i]=='A'){
       totalA += amt_1[i];
   }
   if (type_1[i]=='b' || type_1[i]=='B'){
       totalB += amt_1[i];
   }
   if (type_1[i]=='c' || type_1[i]=='C'){
       totalC += amt_1[i];
   }
 }


 printf("Please enter the %d receipt type and amounts for the #2 employee: \n", n[1]);
 for (int i=0; i<n[1]; i++){
   scanf("%s",&type_2[i]);     
   scanf("%f", &amt_2[i]);
 }
 for (int i=0; i<n[2]; i++){
   sum2 += amt_2[i];
   if (type_2[i]=='a' || type_2[i]=='A'){
       totalA += amt_2[i];
   }
   if (type_2[i]=='b' || type_2[i]=='B'){
       totalB += amt_2[i];
   }
   if (type_2[i]=='c' || type_2[i]=='C'){
       totalC += amt_2[i];
   }
 }


 printf("Please enter the %d receipt type and amounts for the #3 employee: \n", n[2]);
 for (int i=0; i<n[2]; i++){
   scanf("%s",&type_3[i]);    
   scanf("%f", &amt_3[i]);
 }
 for (int i=0; i<n[2]; i++){
   sum3 += amt_3[i];
   if (type_3[i]=='a' || type_3[i]=='A'){
       totalA += amt_3[i];
   }
   if (type_3[i]=='b' || type_3[i]=='B'){
       totalB += amt_3[i];
   }
   if (type_3[i]=='c' || type_3[i]=='C'){
       totalC += amt_3[i];
   }
 }
 
 printf("Employee ID      Amount\n");
 printf("     1           %.2f\n", sum1);
 printf("     2           %.2f\n", sum2);
 printf("     3           %.2f\n\n", sum2);
 printf("Receipt Type     Amount\n");
 printf("     A           %.2f\n", totalA);
 printf("     B           %.2f\n", totalB);
 printf("     C           %.2f\n", totalC);




 return 0;
 
 
 -------------------------------------------------------------------------------------------------------------------------------------
 
 
 
 #2
 
#include <stdio.h>

int main()
{
  int n;
  int r,c;
  printf("Enter how many matrixs do you have: ");
  scanf("%d", &n);
  int sum[n];
  for (int i=0; i<n; i++){
      printf("Enter how many rows and columns are in %d", i+1);
      (i==0)? printf("st "):(i==1)? printf("nd "): (i==2)? printf("rd "):printf("th ");
      printf ("matrix: ");
      scanf("%d%d", &r, &c);

      int a[r][c];
      int total=0;
      printf("Enter each element for %d", i+1);
      (i==0)? printf("st "):(i==1)? printf("nd "): (i==2)? printf("rd "):printf("th ");
       printf ("matrix: \n");

      for (int x=0; x<r; x++){
          for (int y=0; y<c; y++){
              scanf("%d", &a[x][y]);
              if (x==0 || y==0 ||x==r-1||y==c-1) {
                  total += a[x][y];
              }
          }
      }

      sum[i]=total;
  }

  for (int i=0; i<n; i++){
      printf("%d\n", sum[i]);
  }
  return 0;
}



 -------------------------------------------------------------------------------------------------------------------------------------
 
 
 #3
 
 
 #include <stdio.h>

int main()
{
    int r, c;
    printf("Enter the size of row and column: ");
    scanf ("%d%d", &r, &c);

    int a[r][c];
    printf("Enter each of the element in the matrix: \n");
    for (int i=0; i<r; i++){
        for (int j=0; j<c; j++){
            scanf("%d", &a[i][j]);
        }
    }

    int n;
    n=r+c;

    printf("Output: \n");
    for(int x=0; x<=n; x++){
        for (int i=0; i<r; i++){
            for (int j=0; j<c; j++){
               if (i+j == x){
                   printf("%d ", a[i][j]);
               }
            }
        }
    }
    return 0;
}




-------------------------------------------------------------------------------------------------------------------------------------
 
 
 
#4
 
 
#include <stdio.h>

void sort ();

int main()
{
    int n;
    float th;
    printf("Enter how many patients currently there are, and threshold\nvalue of ICU: ");
    scanf("%d %f", &n, &th);
    int id[n];
    float condition[n];

    printf("Enter each patient’s ID and condition indicator: \n");

    for (int i=0; i<n; i++){
        scanf("%d",&id[i]);
        scanf("%f", &condition[i]);
    }
    
    sort (condition, id, n);
    printf("Outputs: \n");

    for (int i=0; i<n; i++){
        if (condition[i]>th){
            printf("%03d %.2f\n", id[i], condition[i]);
        }
    }
   return 0;
}
 
void sort (float condition[], int id[], int n){
	 for (int i=1; i<n; i++){
		 for (int j=0; j<n-i; j++){
			float temp1;
			int temp2;
			if (condition[j]<condition[j+1]){
				temp1 = condition[j];
				condition[j] = condition[j+1];
				condition[j+1] = temp1;
               
				temp2 = id[j];
				id[j] = id[j+1];
				id[j+1] = temp2;
			}
		 }
	 }
 } 
 


-------------------------------------------------------------------------------------------------------------------------------------
 
 
 
#5


#include <stdio.h>

int main()
{
 char a[1000];
 printf("Enter one of strand in a molecule of DNA: : \n");

 gets(a);
 int i=0;
 printf("Output:\nComplementary strand: \n");
 while (a[i]!='\0')
 {
    if (a[i]=='A')
    {
      a[i]='T';
    }
    else if (a[i]=='T')
    {
      a[i]='A';
    }
    else if (a[i]=='G')
    {
      a[i]='C';
    }
    else if (a[i]=='C')
    {
      a[i]='G';
    }
    printf("%c", a[i]);
    i++;
 }

 return 0;
}
